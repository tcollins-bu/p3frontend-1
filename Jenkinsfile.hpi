pipeline {
  agent any
  options {
    skipStagesAfterUnstable()
    disableConcurrentBuilds()
  }
  stages {
    stage ('Hello World') {
      steps {
        echo "hello world"
      }
    }
  }
                              }
